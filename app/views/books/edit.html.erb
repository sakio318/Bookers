<h1>Editing Book </h1>
<%= form_with model:@book, local:true do |f| %>
  <h4>title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <%= f.submit'Update Book' %>
  <a href="">show</a>
  <a href="/books">back</a>
<% end %>
